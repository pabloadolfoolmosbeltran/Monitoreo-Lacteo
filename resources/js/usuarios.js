// ============================================================
// USUARIOS.JS - Lógica del CRUD de usuarios
// ============================================================

document.addEventListener('DOMContentLoaded', function () {
    // Punto de entrada para el CRUD de usuarios.
    // La confirmación de eliminación usa el diálogo nativo (onclick),
    // por lo que no se requiere lógica adicional por ahora.
});
